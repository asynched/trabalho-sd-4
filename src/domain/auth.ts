export const students = [
  // TODO: Get the rest of the usernames
  'asynched',
]

export const teachers = ['mrebello']

export enum Role {
  GUEST = 'guest',
  STUDENT = 'student',
  TEACHER = 'teacher',
}
